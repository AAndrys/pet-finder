export type QueryKeysTypes = {
  authToken: string;
  animals: string;
  animalTypes: string;
};

export const queryKeys: QueryKeysTypes = {
  authToken: 'authToken',
  animals: 'animals',
  animalTypes: 'animalTypes',
};
